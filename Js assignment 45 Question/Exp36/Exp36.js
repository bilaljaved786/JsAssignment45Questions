function make_shirt(size, message) {
  console.log(
    `size of shirt : ${size}, message displaying on shirt : ${message}`
  );
}

make_shirt(42, "Hello Programmers"); // calling the function
